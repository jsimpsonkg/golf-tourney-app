{
  "name": "golf-tourney-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace client",
    "typecheck:api": "tsc -p tsconfig.json"
  },
  "workspaces": [
    "packages/*",
    "client",
    "server"
  ]
}
